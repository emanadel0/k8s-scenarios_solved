apiVersion: v1
kind: Service
metadata:
  name: web-app-svc
spec:
  selector:
    app: web-app        # targets pods with this label
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP        # internal only
